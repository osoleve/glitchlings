# Glitchling Monster Manual

This file is generated by `docs/build_monster_manual.py` so it always reflects the current glitchling defaults. Regenerate it after changing parameters or adding new glitchlings.

## Ekkokin

Glitchling that swaps words for curated homophones.

*"Homophonic idiolectician. There leased favourite flavour? Orange."*

- **Scope:** Word
- **Order:** Early

### Parameters

- `rate` (float | None): default 0.02
- `seed` (int | None): default None

## Hokey

Glitchling that stretches words using linguistic heuristics.

*"Sooooo excited to meet you! We reeeeeally missed you last week."*

- **Scope:** Character
- **Order:** First

### Parameters

- `rate` (float): default 0.3
- `extension_min` (int): default 2
- `extension_max` (int): default 5
- `word_length_threshold` (int): default 6
- `base_p` (float): default 0.45
- `seed` (int | None): default None

## Jargoyle

Glitchling that swaps words using bundled lexeme dictionaries.  Jargoyle replaces words
with alternatives from one of several dictionaries:  - **colors**: Swap color terms
(e.g., "red" → "blue"). - **synonyms**: General synonym substitution (e.g., "fast" →
"rapid"). - **corporate**: Business jargon alternatives. - **academic**: Scholarly word
substitutions.  Two modes are supported:  - **literal**: Use the first (canonical) entry
for each word. - **drift**: Randomly select from available alternatives.  Example:
>>> from glitchlings import Jargoyle     >>> jargoyle = Jargoyle(lexemes="colors",
mode="literal")     >>> jargoyle("The red balloon floated away.")     'The blue balloon
floated away.'      >>> jargoyle = Jargoyle(lexemes="synonyms", mode="drift", rate=0.5,
seed=42)     >>> jargoyle("The quick fox jumps fast.")     'The swift fox jumps rapid.'

*"Oh no... The worst person you know just bought a thesaurus..."*

- **Scope:** Word
- **Order:** Normal

### Parameters

- `lexemes` (str): default "synonyms"
- `mode` (JargoyleMode): default "drift"
- `rate` (float | None): default 0.01
- `seed` (int | None): default None

## Mim1c

Glitchling that swaps characters for visually similar homoglyphs.

*"Breaks your parser by replacing some characters in strings with doppelgangers. Don't worry, this text is clean. ;)"*

- **Scope:** Character
- **Order:** Last

### Parameters

- `rate` (float | None): default 0.02
- `classes` (list[str] | Literal['all'] | None): default None
- `banned_characters` (Collection[str] | None): default None
- `seed` (int | None): default None

## Pedant

Glitchling that deterministically applies pedant evolutions.

*"Resurrects archaic ligatures and diacritics."*

- **Scope:** Word
- **Order:** Late

### Parameters

- `stone` (PedantStone | str): default Coeurite
- `seed` (int | None): default None

## Redactyl

Glitchling that redacts words with block characters.

*"Some things are better left ████████."*

- **Scope:** Word
- **Order:** Normal

### Parameters

- `replacement_char` (str): default "█"
- `rate` (float | None): default 0.025
- `merge_adjacent` (bool): default false
- `seed` (int): default 151
- `unweighted` (bool): default false

## Rushmore

Glitchling that bundles deletion, duplication, and swap attacks.

*"You shouldn't have waited for the last minute to write that paper, anon. Sure hope everything is in the right place."*

- **Scope:** Word
- **Order:** Normal

### Parameters

- `name` (str): default "Rushmore"
- `modes` (RushmoreMode | str | Iterable[RushmoreMode | str] | None): default (<RushmoreMode.DELETE: 'delete'>,)
- `rate` (float | None): default None
- `delete_rate` (float | None): default None
- `duplicate_rate` (float | None): default None
- `swap_rate` (float | None): default None
- `seed` (int | None): default None
- `unweighted` (bool): default false
- `delete_unweighted` (bool | None): default None
- `duplicate_unweighted` (bool | None): default None

## Scannequin

Glitchling that simulates OCR artifacts using common confusions.

*"Isn't it weird how the word 'bed' looks like a bed?"*

- **Scope:** Character
- **Order:** Late

### Parameters

- `rate` (float | None): default 0.02
- `seed` (int | None): default None

## Typogre

Glitchling that introduces deterministic keyboard-typing errors.

*"What a nice word, would be a shame if something happened to it..."*

- **Scope:** Character
- **Order:** Early

### Parameters

- `rate` (float | None): default 0.02
- `keyboard` (str): default "CURATOR_QWERTY"
- `seed` (int | None): default None

## Zeedub

Glitchling that plants zero-width glyphs inside words.

*"I'm invoking my right to remain silent."*

- **Scope:** Character
- **Order:** Last

### Parameters

- `rate` (float | None): default 0.02
- `seed` (int | None): default None
- `characters` (Sequence[str] | None): default None
